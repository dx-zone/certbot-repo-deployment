###############################################################################
# {{ ansible_managed }}
#
# SOURCE: {{ ansible_parent_role_paths[0] | default('N/A') }}/templates/{{ template_path | basename }}
# DATE  : {{ ansible_facts.date_time.iso8601 }}
# HOST  : {{ inventory_hostname }}
# USER  : {{ ansible_facts.user_id }}
#
# WARNING: Manual changes to this file will be lost the next time
#          the Ansible playbook is executed.
###############################################################################

# ==============================================================================
# 🚀 PROJECT INFRASTRUCTURE CONFIGURATION (.env)
# ==============================================================================

# 🌐 REPO_FQDN: Primary domain for the RPM Repository.
# 1) rpmrepo (Apache): ServerName + TLS cert paths
# 2) MUST exist in certificates.csv for Let's Encrypt issuance
# 3) client-test: repository hostname target
REPO_FQDN="{{ certbot_stack_repo_fqdn }}"

# 🛡️ CA_NAME: CN label for the internal Client-Auth Root CA (trust store display)
CA_NAME="{{ certbot_stack_ca_name }}"

# 👤 CLIENT_NAME: identity/prefix for the client certificate files
CLIENT_NAME="{{ certbot_stack_client_name }}"

# ==============================================================================
# ⚠️ Maintenance Notes
# - Changing REPO_FQDN may require cert re-issuance and container restart
# - Changing CA_NAME/CLIENT_NAME requires regenerating mTLS material
# ==============================================================================
